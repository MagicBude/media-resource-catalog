# V0.2.2 database package export follow-up

- 为 `@media-resource-catalog/database` 增加正式 package `exports` 入口。
- 修复 API 工作区中 ESLint type-aware 分析无法解析
  `createDatabase`、`MediaRepository` 与数据库 client 类型的问题。
- 不关闭 `no-unsafe-*` ESLint 规则，不改变数据库运行行为。
