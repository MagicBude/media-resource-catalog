{
  "name": "media-resource-catalog",
  "version": "0.2.2",
  "private": true,
  "packageManager": "pnpm@11.25.0",
  "scripts": {
    "dev": "pnpm -r --parallel --filter @media-resource-catalog/web --filter @media-resource-catalog/api dev",
    "build": "pnpm -r --filter @media-resource-catalog/core --filter @media-resource-catalog/database --filter @media-resource-catalog/providers --filter @media-resource-catalog/catalog --filter @media-resource-catalog/api --filter @media-resource-catalog/web build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "repo:validate": "node scripts/validate-repository.mjs",
    "check": "pnpm repo:validate && pnpm lint && pnpm typecheck && pnpm test && pnpm build",
    "db:generate": "pnpm --filter @media-resource-catalog/database db:generate",
    "db:migrate": "pnpm --filter @media-resource-catalog/database db:migrate",
    "db:studio": "pnpm --filter @media-resource-catalog/database db:studio",
    "media:import": "pnpm --filter @media-resource-catalog/api media:import"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@types/node": "24.10.1",
    "eslint": "10.10.0",
    "globals": "16.5.0",
    "typescript": "5.9.3",
    "typescript-eslint": "8.69.0",
    "vitest": "5.0.0"
  }
}
